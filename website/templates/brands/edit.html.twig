{% extends 'base.html.twig' %}

{% block title %}Edit Brands{% endblock %}

{% block content %}
    <h1>Edit Brands</h1>

    {{ include('brands/_form.html.twig', {'button_label': 'Update'}) }}
    <ul class="actions small">
        <li><a href="{{ path('brands_index') }}" class="button">back to list</a></li>
        <li>{{ include('brands/_delete_form.html.twig') }}</li>
    </ul>
{% endblock %}
