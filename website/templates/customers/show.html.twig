{% extends 'base.html.twig' %}

{% block title %}Customers{% endblock %}

{% block content %}
    <h1>Customers</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CustomerId</th>
                <td>{{ customer.customerId }}</td>
            </tr>
            <tr>
                <th>CustomerFirstName</th>
                <td>{{ customer.customerFirstName }}</td>
            </tr>
            <tr>
                <th>CustomerSecondName</th>
                <td>{{ customer.customerSecondName }}</td>
            </tr>
            <tr>
                <th>CustomerPhone</th>
                <td>{{ customer.customerPhone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('customers_index') }}">back to list</a>

    <a href="{{ path('customers_edit', {'customerId': customer.customerId}) }}">edit</a>

    {{ include('customers/_delete_form.html.twig') }}
{% endblock %}
