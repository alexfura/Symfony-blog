{% extends "base.html.twig" %}

{% block title %}
    {{ post.title }}
{% endblock title %}


{% block content %}
    <h1>{{ post.title }}</h1>
    <p>{{ post.textfield }}</p>

    <ul class="actions">
        <li><a class="button big" href="{{ request->get }}">Back</a></li>
    </ul>
{% endblock content %}